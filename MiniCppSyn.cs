// MiniCppSyn.cs                                              HDO, 2006-08-28
// -------------
// Table-driven top-down syntax analyzer (G-code interpreter).
// Generated by Coco-2 (PGT).
//=====================================|========================================

// --- call pragma mehts. and exec. sem. acts. after syntax errors? ---
#define CONTPRAGMAACTS            // !!!must be defined for SG and PGT!!! 
#undef  CONTSEMACTS               // should not be defined 

using System;
using System.Text;

using Lex = MiniCppLex;
using Sem = MiniCppSem;

public class MiniCppSyn {
                   
  public static String MODULENAME = "MiniCppSyn";

  // --- G-code instructions ---
  public enum Instruction {
    T, TA, TC, TCA, NT, NTA, ANY, ANYA, EPS, EPSA, JMP, RET
  } // Instruction

  private const int ROOTPC     =  916;
  private const int LASTTERM   =   50;
  private const int LASTPRAGMA =   50;


  // --- other constants ---
  private const int MINERRDIST   =   3;
  private const int MAXSTACKLEN  = 100;
  private const int EOFTOK       =   0;
  private const int FIRSTNONTERM = LASTPRAGMA + 1;

  public class NTData {
    public int          startPC;
    public bool         del;
    public Sets.Set256  firstSet;
    public NTData(int startPC, bool del, Sets.Set256 firstSet) {
      this.startPC  = startPC;
      this.del      = del;
      this.firstSet = firstSet;
    } // NTData
  } // NTData
  
  private class GrammarTable {
    public int[]  header = {
      921, 50, 50, 80, 19, 0
    };
    public byte[] code = {
        13,   5,   1,  10,   0,   2,  10,   2,   0,   5,   3,  18,   0,   3,
        10,   2,   0,   8,   1,  16,  11,   0,   4,   4,   7,   1,   2,  48,
         2,  15,   4,   2,   4,   0,  46,  11,   0,  45,   1,   9,  48,   0,
        13,   8,   2,  18,  11,   1,  14,  56,   0,  14,  10,  44,   0,   1,
        20,  67,   0,   2,  49,   4,  17,  10,  44,   0,   1,  21,  60,   0,
        15,  10,  60,   0,   4,   7,   1,   1,  22,  83,   0,  14,   2,  48,
         3,   9,   3,  95,   0,  16,   4,   5,   5,  11,   8,   4,  18,   0,
        27,   5,   6, 105,   0,   7,   0,  28,   9,   5, 115,   0,  20,   0,
        46,  11,   8,   6,  21,   4,   8,  10,  23,  11,   4,   7,   1,   1,
        22, 131,   0,  14,   2,  48,   3,  16,   4,   5,   5,  11,   5,   2,
       145,   0,   1,  14,   8,   2,  15,   1,  44, 173,   0,   1,  22, 157,
         0,  16,   2,  48,   5,  18,   5,   2, 167,   0,   7,  20,   8,   2,
        21,  10, 148,   0,   8,   5,  22,   0,  46,  11,   4,   7,   1,   9,
         7, 188,   0,  14,  11,   1,  22, 193,   0,  15,   2,  48,   4,   1,
        29, 203,   0,   0,  30,  17,   8,   8,  18,   1,  44, 234,   0,   4,
         7,   7,   1,  22, 218,   0,  20,   2,  48,   9,   1,  29, 228,   0,
         0,  30,  22,   8,   8,  23,  10, 206,   0,   8,   7,  11,   1,  15,
       243,   0,  13,  11,   1,   1, 249,   0,  14,  11,   0,  11,  15,  11,
         0,  31,   5,   1,   8,   1,   1,  16,  10, 255,   0,   5,   4,  16,
         1,   2,  10,   4,   1,   5,   9,  24,   1,   3,  10,   4,   1,   8,
         9,  17,   0,  32,  11,   5,  10,  36,   1,   1,  11,   5,  12,  42,
         1,   2,  11,   5,  13,  48,   1,   3,  11,   5,  19,  54,   1,   4,
        11,   5,  14,  60,   1,   5,  11,   5,  15,  66,   1,   6,  11,   5,
        16,  72,   1,   7,  11,   5,  17,  78,   1,   8,  11,   5,  18,  84,
         1,   9,  11,   9,   6,  94,   1,  22,   4,   8,  11,  24,  11,   0,
        46,  25,  11,   2,  48,   1,  14,   1,  25, 110,   1,  15,   0,  46,
        11,   1,  26, 118,   1,  16,  10, 107,   1,   9,  10, 133,   1,  17,
         0,  45,  21,   4,  22,  10,  23,  10, 107,   1,   1,  29, 147,   1,
        18,   4,  22,   7,   0,  30,  20,  10, 123,   1,   0,  27,  24,   5,
        11, 155,   1,  13,   0,  28,  26,  10, 107,   1,   4,  22,   1,  14,
         1,  44, 176,   1,   4,  22,   3,  16,  10, 165,   1,   8,   7,  11,
         0,  10,  13,   0,  27,   4,  22,   2,   0,  28,   4,   9,   3,   1,
         7, 199,   1,   4,   9,   4,   8,  11,  17,  11,   0,  16,  13,   0,
        27,   4,  22,   2,   0,  28,  15,   4,   9,   4,  17,  11,   0,   2,
        13,   0,  46,  11,   0,   3,  13,   0,  43,   2,  48,   2,  15,   0,
        46,  11,   0,   5,  13,   0,  42,   5,  22,  26,   2,   2,  15,   1,
        42,  20,   2,   5,  22,   5,   2,   7,  20,  10, 248,   1,   3,  50,
        14,   2,   9,  22,  10, 248,   1,   0,   8,  23,  10, 248,   1,   8,
         5,  24,   0,  46,  11,   3,  50,  35,   2,   4,  17,  10, 248,   1,
         0,   8,  18,  10, 248,   1,   0,   6,  13,   0,  29,   0,  30,   2,
        48,   2,  15,   0,  46,  11,   0,  13,  13,   5,  22,  63,   2,   2,
         8,   5,  15,   0,  46,  11,   0,  17,   0,  27,   4,  22,   1,   0,
        28,  14,   0,  31,   5,  20,  89,   2,   3,  10,  81,   2,   5,  21,
        94,   2,   4,   0,  32,  17,  11,   0,  18,   2,  49,   1,   0,  47,
         5,   9, 113,   2,   2,  10, 105,   2,   8,  12,  11,   0,  19,   0,
        47,   4,   9,   1,   5,   9, 131,   2,   2,  10, 123,   2,   8,   9,
        11,   4,  23,   1,  11,   4,  24,   1,  14,   1,  34, 154,   2,  15,
         4,  24,   4,  17,  10, 142,   2,   8,  13,  11,   4,  25,   1,  14,
         1,  35, 173,   2,  15,   4,  25,   4,  17,  10, 161,   2,   8,  14,
        11,   4,  26,   1,  14,   1,  36, 190,   2,  15,   4,  26,   9,  22,
        11,   1,  37, 198,   2,  16,  10, 185,   2,   1,  38, 206,   2,  17,
        10, 185,   2,   1,  39, 214,   2,  18,  10, 185,   2,   1,  40, 222,
         2,  19,  10, 185,   2,   1,  41, 230,   2,  20,  10, 185,   2,   8,
        15,  11,   1,  20,   9,   3,  13,   4,  27,   3,  16,   1,  20, 254,
         2,  17,   4,  27,   7,  20,  10, 242,   2,   1,  21,   6,   3,  18,
        10, 247,   2,   8,  16,  11,   1,  21, 238,   2,  14,  10, 238,   2,
         4,  28,   1,  14,   1,  22,  33,   3,  15,   4,  28,   6,  19,  10,
        21,   3,   1,  23,  41,   3,  16,  10,  26,   3,   1,  24,  49,   3,
        17,  10,  26,   3,   8,  17,  11,   1,  33,  57,   3,  13,   4,  29,
         2,  15,  11,   1,   9,  68,   3,  13,  11,   1,  14,  74,   3,  14,
        11,   3,  49,  81,   3,   3,  16,  11,   3,  48, 122,   3,   5,  18,
         9,  18,  93,   3,  19,  11,   9,  19, 108,   3,  20,   0,  29,  21,
         4,  22,  10,   0,  30,  23,  11,   8,   4,  24,   0,  27,   5,  11,
       118,   3,  13,   0,  28,  26,  11,   1,  12, 139,   3,  27,   4,   7,
        16,   0,  29,   4,  22,  17,   0,  30,  30,  11,   0,  27,   4,  22,
        19,  32,   0,  28,  11,   4,   0,   1,   0,   0,  11
    };
    public NTData[] nts = {
      new NTData(   1, true , new Sets.Set256(0x8812)),
      new NTData(  22, false, new Sets.Set256(0x0010)),
      new NTData(  37, false, new Sets.Set256(0x0000, 0x0000, 0x2000)),
      new NTData(  75, false, new Sets.Set256(0x8802)),
      new NTData( 123, false, new Sets.Set256(0x8802)),
      new NTData( 139, false, new Sets.Set256(0x0000, 0x0000, 0x7000)),
      new NTData( 179, false, new Sets.Set256(0x8802)),
      new NTData( 237, false, new Sets.Set256(0x8802)),
      new NTData( 253, false, new Sets.Set256(0x0000, 0x8000)),
      new NTData( 286, false, new Sets.Set256(0x246c, 0x8003, 0x4000, 0x0001)),
      new NTData( 354, false, new Sets.Set256(0x0000, 0x0000, 0x0000, 0x0001)),
      new NTData( 417, false, new Sets.Set256(0x5200, 0x0830, 0x0002, 0x0003)),
      new NTData( 435, false, new Sets.Set256(0x0400)),
      new NTData( 459, false, new Sets.Set256(0x0000, 0x0001)),
      new NTData( 475, false, new Sets.Set256(0x0004)),
      new NTData( 481, false, new Sets.Set256(0x0008)),
      new NTData( 493, false, new Sets.Set256(0x0020)),
      new NTData( 553, false, new Sets.Set256(0x0040)),
      new NTData( 567, false, new Sets.Set256(0x2000)),
      new NTData( 581, false, new Sets.Set256(0x0000, 0x0002)),
      new NTData( 610, false, new Sets.Set256(0x0000, 0x0004)),
      new NTData( 628, false, new Sets.Set256(0x0000, 0x0008)),
      new NTData( 646, false, new Sets.Set256(0x5200, 0x0830, 0x0002, 0x0003)),
      new NTData( 650, false, new Sets.Set256(0x5200, 0x0830, 0x0002, 0x0003)),
      new NTData( 669, false, new Sets.Set256(0x5200, 0x0830, 0x0002, 0x0003)),
      new NTData( 688, false, new Sets.Set256(0x5200, 0x0830, 0x0002, 0x0003)),
      new NTData( 745, false, new Sets.Set256(0x5200, 0x0830, 0x0002, 0x0003)),
      new NTData( 785, false, new Sets.Set256(0x5200, 0x0800, 0x0002, 0x0003)),
      new NTData( 820, false, new Sets.Set256(0x5200, 0x0800, 0x0002, 0x0003)),
      new NTData( 830, false, new Sets.Set256(0x5200, 0x0800, 0x0000, 0x0003))
    };
    public Sets.Set256[] epsSets = {
      new Sets.Set256(0x0001),
      new Sets.Set256(0x0000, 0x0000, 0x5000),
      new Sets.Set256(0x0000, 0x0000, 0x7000),
      new Sets.Set256(0x0000, 0x0800),
      new Sets.Set256(0x0000, 0x0000, 0x4000),
      new Sets.Set256(0x0000, 0x8000),
      new Sets.Set256(0x0000, 0x1000),
      new Sets.Set256(0x0000, 0x1000, 0x1000),
      new Sets.Set256(0x0000, 0x0000, 0x0001),
      new Sets.Set256(0x0000, 0x0000, 0x2000),
      new Sets.Set256(0xacfe, 0x800f, 0x4001, 0x0001),
      new Sets.Set256(0x0000, 0x000c, 0x0001),
      new Sets.Set256(0x0000, 0x5000, 0x5400),
      new Sets.Set256(0x0000, 0x5000, 0x5404),
      new Sets.Set256(0x0000, 0x5000, 0x540c),
      new Sets.Set256(0x0000, 0x5000, 0x57fc),
      new Sets.Set256(0x0000, 0x5030, 0x57fc),
      new Sets.Set256(0x0000, 0x51f0, 0x57fc),
      new Sets.Set256(0x0000, 0x2000)
    };
    public Sets.Set256[] anySets = {
      new Sets.Set256(0x0000)
    };
    public String[] names = {
      "end of file", "bool", "break", "cin", "const", "cout", "delete",
      "else", "endl", "false", "if", "int", "new", "return", "true",
      "void", "while", "switch", "case", "default", "+", "-", "*", "/",
      "%", "++", "--", "(", ")", "[", "]", "{", "}", "!", "||", "&&", "==",
      "!=", "<", "<=", ">", ">=", "<<", ">>", ",", "=", ";", ":", "ident",
      "number", "string", "MiniCpp", "ConstDecl", "Init",
      "VarDefOrFuncDeclOrDef", "VarDef", "VarDefRest", "FormParList",
      "Type", "Block", "Stat", "IncDecAssignOrCallStat", "ActParList",
      "IfStat", "WhileStat", "BreakStat", "InputStat", "OutputStat",
      "DeleteStat", "ReturnStat", "SwitchStat", "CaseList", "DefaultPart",
      "Expr", "OrExpr", "AndExpr", "RelExpr", "SimpleExpr", "Term",
      "NotFact", "Fact"
    };
  } // GrammarTable;

  private static GrammarTable gt;

  private static int   pc;             // G-code counter
  private static int[] stk;            // stack of G-code return adresses
  private static int   sp;             // stack pointer
  private static bool  mustRead;       // next Lex.token to read
  private static int   altRoot;        // root of alternative chain
  private static bool  leaveOrEnter;   // leaveCnt > 0 OR enterCnt > 0
  private static int   leaveCnt;       // nr. of sem. proc. to leave
  private static int   enterCnt;       // nr. of sem. proc. to enter
  private static int   errDist;        // current error distance
  private static int[] stkAtErr;       // saved stack at error position
  private static int[] newSP;          // new stack length
  private static int[] newPC;          // pc after recovery


  public static void MiniCppSynMethod(Utils.ModuleAction action, out String moduleName) {
  //-----------------------------------|----------------------------------------
    moduleName = MODULENAME;
    switch (action) {
      case Utils.ModuleAction.getModuleName:
        return;
      case Utils.ModuleAction.initModule:
        Errors.InstallStopParsingMethod(StopParsing);
        gt       = new GrammarTable();
        stk      = new int[MAXSTACKLEN];
        stkAtErr = new int[MAXSTACKLEN];
        newSP    = new int[LASTTERM + 1];
        newPC    = new int[LASTTERM + 1];
        break;
      case Utils.ModuleAction.resetModule:
        break;
      case Utils.ModuleAction.cleanupModule:
        gt       = null;
        stk      = null;
        stkAtErr = null;
        newSP    = null;
        newPC    = null;
        break;
    } // switch
  } // MiniCppSynMethod

  private static void Push(int pc) {
    if (sp < MAXSTACKLEN)
      stk[sp++] = pc;
    else
      Errors.Restriction(MODULENAME, "Push", "parser stack overflow");
  } // Push

  private static int Pop() {
    if (sp > 0)
      return stk[--sp];
    else {
      Errors.CompilerError(MODULENAME, "Pop", "parser stack underflow");
      return 0;
    } // else
  } // Pop

  private static void GetNextToken() {
    for (;;) {
      Lex.GetToken(); // updates Lex.token
      if (Lex.token <= LASTTERM)
        return;
  #if !CONTPRAGMAACTS
      if (NumOfSynErrors() == 0)
  #endif
        Sem.pragmaMethods[Lex.token - LASTTERM - 1]();
    } // for
  } // GetNextToken

  private static int AdrAt(int pc) {
    return (gt.code[pc - 1] |
           (gt.code[pc    ] << 8));
  } // AdrAt

  private static void AdjustPC(ref int pc) {
    if (pc == 0)
      return;
    for (;;)
      switch ((Instruction)gt.code[pc - 1]) {
        case Instruction.T:
        case Instruction.TA:
        case Instruction.TC:
        case Instruction.TCA:
        case Instruction.NT:
        case Instruction.NTA:
        case Instruction.ANY:
        case Instruction.ANYA:
        case Instruction.EPS:
        case Instruction.EPSA:
          return;
        case Instruction.JMP:
          pc = AdrAt(pc + 1);
          break;
        case Instruction.RET:
          pc = 0;
          return;
        default:
          pc++; // syn. or sem. action
          break;
      } // switch
  } // AdjustPC

  private static void GetSymInstr(int pc, 
                        out int opcode, out int sy, out int nextPC, out int altPC) {
    opcode = gt.code[pc - 1];
    nextPC = 0; // dummy init
    altPC  = 0; // dummy init 
    switch ((Instruction)opcode) {
      case Instruction.T:
      case Instruction.TA:
      case Instruction.TC:
      case Instruction.TCA:
      case Instruction.ANYA:
      case Instruction.EPS:
      case Instruction.EPSA:
        sy = gt.code[pc];
        break;
      case Instruction.NT:
      case Instruction.NTA:
        sy = gt.code[pc] + FIRSTNONTERM;
        break;
      default: // ANY, JMP, RET, SEM, SYN 
        sy = 0;
        break;
    } // switch
    switch ((Instruction)opcode) {
      case Instruction.T:
      case Instruction.EPS:
        nextPC = pc + 2;
        altPC = 0;
        break;
      case Instruction.TC:
      case Instruction.NT:
        nextPC = pc + 3;
        altPC = 0;
        break;
      case Instruction.TA:
      case Instruction.ANYA:
      case Instruction.EPSA:
        nextPC = pc + 4;
        altPC = AdrAt(pc + 2);
        break;
      case Instruction.TCA:
      case Instruction.NTA:
        nextPC = pc + 5;
        altPC = AdrAt(pc + 2);
        break;
      case Instruction.ANY:
        nextPC = pc + 1;
        altPC = 0;
        break;
    } // switch
    AdjustPC(ref nextPC);
    AdjustPC(ref altPC);
  } // GetSymInstr

  private static void Fill(int pc, int sp) {
    int opcode, sy, nextPC, altPC, i;
    AdjustPC(ref pc);
    while (pc > 0) {
      GetSymInstr(pc, out opcode, out sy, out nextPC, out altPC);
      switch ((Instruction)opcode) {
        case Instruction.T:
        case Instruction.TA:
        case Instruction.TC:
        case Instruction.TCA:
          newPC[sy] = pc;
          newSP[sy] = sp;
          break;
        case Instruction.NT:
        case Instruction.NTA:
          for (i = 0; i <= LASTTERM; i++)
            if (Sets.member((ushort)i, gt.nts[sy - FIRSTNONTERM].firstSet)) {
              newPC[i] = pc;
              newSP[i] = sp;
            }
          if (gt.nts[sy - FIRSTNONTERM].del)
            Fill(nextPC, sp);
          break;
        case Instruction.EPS:
        case Instruction.EPSA:
          Fill(nextPC, sp);
          break;
      } // switch
      pc = altPC;
    } // while
  } // Fill

  private static void FillSucc(int pc, int sp) {
    int opcode, sy, nextPC, altPC;
    AdjustPC(ref pc);
    while (pc > 0) {
      GetSymInstr(pc, out opcode, out sy, out nextPC, out altPC);
      if (nextPC > 0)
        Fill(nextPC, sp);
      pc = altPC;
    } // while
  } // FillSucc

  private static void Triple(int altRoot) {
    for (int i = 0; i <= LASTTERM; i++) { // clear triple list
      newPC[i] = 0;
      newSP[i] = 0;
    } // for
    for (int i = 1; i <= sp; i++) {       // successors of stacked nt's
      FillSucc(stk[i - 1], i - 1);
      Fill(stk[i - 1], i - 1);
    } // for
    FillSucc(altRoot, sp);                // successors of alt. chain
    Fill(altRoot, sp);                    // current alt. chain
  } // Triple

  private static void HandleSynErr(ref int pc, ref int altRoot) {
    int     opcode, sy, nextPC, altPC, adjPC, nwSP, eq;
    bool    nameInMsg;
    String  name;
    StringBuilder msg = new StringBuilder();
    leaveOrEnter = false;
    if (errDist >= MINERRDIST) { // --- primary error
      nameInMsg = false;
      adjPC = altRoot;
      AdjustPC(ref adjPC);
      while (adjPC > 0) {
        GetSymInstr(adjPC, out opcode, out sy, out nextPC, out altPC);
        if ( (Instruction)opcode >= Instruction.T  && 
             (Instruction)opcode <= Instruction.NTA  ) {
          if ( gt.names[sy].IndexOf(' ') >= 0 || 
               !Char.IsLetter(gt.names[sy][0]) )
            name = "\"" + gt.names[sy] + "\"";
          else
            name = gt.names[sy];
          if (name.IndexOf('{') > -1) // prevent formatting problems in Errors.SynError
            name = name.Replace("{", "{{");
          if (name.IndexOf('}') > -1) // prevent formatting problems in Errors.SynError
            name = name.Replace("}", "}}");
          if (msg.Length + name.Length + 2 > 80) {
            msg.Append(" ...");
            break;
          } // if
          if (!nameInMsg) {
            nameInMsg = true;
            msg.Append("expected: " + name);
          } else {
            msg.Append(", ");
            msg.Append(name);
          } // else
        } // if
        adjPC = altPC;
      } // while
      if (!nameInMsg)
        msg.Append("syntax error");
      Errors.SynError(Lex.tokenLine, Lex.tokenCol, msg.ToString(), "");
      Triple(altRoot);
      while (newPC[Lex.token] == 0) {
        GetNextToken();      // may result in a call to StopParsing
        if (leaveOrEnter)
          return;
      } // while
      nwSP = newSP[Lex.token];
      leaveCnt = sp - nwSP;
      enterCnt = 0;
      Array.Copy(stk, stkAtErr, stk.Length); // save stack
    } else {                 // --- spurios error
      if (errDist == 0) {    // no token used since last error
        GetNextToken();      // may result in a call to StopParsing
        if (leaveOrEnter)
          return;
      } // if
      while (newPC[Lex.token] == 0) {
        GetNextToken();      // may result in a call to StopParsing
        if (leaveOrEnter) 
          return;
      } // while
      nwSP = newSP[Lex.token];
      eq = 2;                // compare stkAtErr and stk
      while (eq <= sp && eq <= nwSP && stkAtErr[eq - 1] == stk[eq - 1])
        eq++;
      eq--;                  // stacks are equal up to index eq
      leaveCnt = sp - eq;
      if (nwSP < eq) {
        leaveCnt = sp - nwSP;
        enterCnt = 0;
      } else if (nwSP > eq)
        enterCnt = nwSP - eq;
      else // nwSP == eq
        enterCnt = 0;
      Array.Copy(stkAtErr, stk, stk.Length); // restore stack
    } // else
    leaveOrEnter = (leaveCnt + enterCnt) > 0;
    sp = nwSP;
    pc = newPC[Lex.token];
    altRoot = pc;
    errDist = 0;
  } // HandleSynErr

  public static int Interpret() {
  //-----------------------------------|----------------------------------------
    int actionNr, opcode, currPC;
    for (;;) {
      opcode = gt.code[pc - 1];
      if ( !leaveOrEnter && mustRead && 
           (Instruction)opcode <= Instruction.EPSA ) {
        GetNextToken();
        mustRead = false;
        errDist++;
        altRoot = pc;
      } // if
      if (leaveOrEnter) {
        if (leaveCnt > 0) {
          leaveCnt--;
          actionNr = 0;
        } else { // enterCnt > 0
          enterCnt--;
          actionNr = gt.code[stkAtErr[sp - enterCnt - 1] - 2];
        } // else
        leaveOrEnter = (leaveCnt + enterCnt) > 0;
        return actionNr;
      } // if
      pc++;
      switch ((Instruction)opcode) {
        case Instruction.T:
          if (Lex.token == gt.code[pc - 1])
            if (Lex.token != EOFTOK) {
              pc++;
              mustRead = true;
            } else {
              return 0;
            } // else
          else
            HandleSynErr(ref pc, ref altRoot);
          break;
        case Instruction.TA:
          if (Lex.token == gt.code[pc - 1]) {
            pc += 3;
            mustRead = true;
          } else
            pc = AdrAt(pc + 1); // try alternative
          break;
        case Instruction.TC:
          if (Lex.token == gt.code[pc - 1]) {
            pc += 2;
            mustRead = true;
            actionNr = gt.code[pc - 2];
            return actionNr;
          } else
            HandleSynErr(ref pc, ref altRoot);
          break;
        case Instruction.TCA:
          if (Lex.token == gt.code[pc - 1]) {
            pc += 4;
            mustRead = true;
            actionNr = gt.code[pc - 2];
            return actionNr;
          } else
            pc = AdrAt(pc + 1); // try alternative
          break;
        case Instruction.NT:
          currPC = gt.code[pc - 1];
          if ( gt.nts[currPC].del || 
               Sets.member((ushort)Lex.token, gt.nts[currPC].firstSet) ) {
            pc++;
            actionNr = gt.code[pc - 1];
            Push(pc + 1);
            pc = gt.nts[currPC].startPC;
            altRoot = pc;
            return actionNr;
          } else
            HandleSynErr(ref pc, ref altRoot);
          break;
        case Instruction.NTA:
          currPC = gt.code[pc - 1];
          if (Sets.member((ushort)Lex.token, gt.nts[currPC].firstSet)) {
            pc += 3;
            actionNr = gt.code[pc - 1];
            Push(pc + 1);
            pc = gt.nts[currPC].startPC;
            altRoot = pc;
            return actionNr;
          } else
            pc = AdrAt(pc + 1);
          break;
        case Instruction.ANY:
          mustRead = true;
          break;
        case Instruction.ANYA:
          currPC = gt.code[pc - 1];
          if (Sets.member((ushort)Lex.token, gt.anySets[currPC - 1])) {
            pc += 3;
            mustRead = true;
          } else
            pc = AdrAt(pc + 1);
          break;
        case Instruction.EPS:
          currPC = gt.code[pc - 1];
          if (Sets.member((ushort)Lex.token, gt.epsSets[currPC - 1]))
            pc++;
          else
            HandleSynErr(ref pc, ref altRoot);
          break;
        case Instruction.EPSA:
          currPC = gt.code[pc - 1];
          if (Sets.member((ushort)Lex.token, gt.epsSets[currPC - 1]))
            pc += 3;
          else
            pc = AdrAt(pc + 1);
          break;
        case Instruction.JMP:
          pc = AdrAt(pc);
          break;
        case Instruction.RET:
          pc = Pop();
          altRoot = pc;
          return 0;
        default:
          if (opcode <= 128) {
  #if !CONTSEMACTS
          if (Errors.NumOfSynErrors() == 0)
  #endif
            return opcode - 12;
          } else // opcode > 128
            return opcode - 128;
          break;
      } // switch
    } // for
  } // Interpret


  public static bool Parse() {
  //-----------------------------------|----------------------------------------
    Lex.InitLex();
    sp           = 0;
    pc           = ROOTPC;
    altRoot      = pc;
    mustRead     = true;
    errDist      = MINERRDIST + 1;
    leaveOrEnter = false;
    leaveCnt     = 0;
    enterCnt     = 0;
    Sem.StartSem();
    return Errors.NumOfSynErrors() == 0;
  } // Parse


  public static void StopParsing() {
  //-----------------------------------|----------------------------------------
    leaveCnt     = sp + 1;
    enterCnt     = 0;
    leaveOrEnter = true;
  } // StopParsing

} // MiniCppSyn
 
// End of MiniCppSyn.cs
//=====================================|========================================